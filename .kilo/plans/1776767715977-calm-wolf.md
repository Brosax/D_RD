# Implementation Plan: Replace Welcome Screen with DEKRA R&D ASCII Art

## Goal
Replace the default "Claude Code" welcome ASCII art and message with a dark green "DEKRA R&D" ASCII art.

## Steps

1. **Update `src/components/LogoV2/WelcomeV2.tsx`**:
   - Change the `welcomeMessage` variable from `'Welcome to Claude Code'` to `'Welcome to DEKRA R&D'`.
   - In all returned `<Box>` components (handling light theme, dark theme, and Apple Terminal specifically), replace the existing Claude Code ASCII art (the "clawd" graphics) with the new DEKRA R&D ASCII art.
   - Example ASCII art to use:
     ```text
       ___  ____  __ __  ___   ___      ___   ___   ___ 
      / _ \/ __/ / //_/ / _ \ / _ |    / _ \ ( _ ) / _ \
     / // / _/  / ,<   / , _// __ |   / , _// _ \// // /
    /____/___/ /_/|_| /_/|_|/_/ |_|  /_/|_| \___//____/ 
     ```
   - Apply a dark green color to the ASCII art by wrapping the text strings in `<Text color="#006400">` or a similar appropriate dark green hex code/Ink color.
   - Adjust the spacing and width variables (like `WELCOME_V2_WIDTH`) if the new ASCII art is wider or taller than the previous one, ensuring it displays properly within the terminal constraints.

2. **Verify UI Framework Compatibility**:
   - The UI is built using the Ink framework located in `packages/@ant/ink/`. The standard `<Text>` component supports `color` attributes, which can take hex values or predefined theme colors. Ensure the chosen dark green is readable in both light and dark terminal themes.

## Next Steps
Once this plan is approved, I will implement these changes in `src/components/LogoV2/WelcomeV2.tsx` and run the development build to verify the output formatting and colors.